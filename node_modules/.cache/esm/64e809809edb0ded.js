let artistsSchema,mongoose;_9ed‍.x([["default",()=>_9ed‍.o]]);_9ed‍.w("./artists.model",[["default",["artistsSchema"],function(v){artistsSchema=v}]]);_9ed‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);


artistsSchema.statics = {
    create: function(data, cb) {
        let artist = new this(data);
        artist.save(cb);
    },

    get: function(query, cb) {
        this.find(query, cb);
    },

    getByName: function(query, cb) {
        this.find(query, cb);
    },

    update: function(query, updateData, cb) {
        this.findOneAndUpdate(query, { $set: updateData }, { new: true }, cb);
    },

    delete: function(query, cb) {
        this.findOneAndDelete(query, cb);
    }
};

const artistsModel = mongoose.model('Artists', artistsSchema);
_9ed‍.d(artistsModel);
